# Agent Routing

Use `react-view-transition/SKILL.md` as the canonical React View Transition skill in this repo.

## When to Apply This Skill

Apply it for any request mentioning one or more of these:

- `ViewTransition` component
- `view-transition-name`
- `startViewTransition`
- enter, exit, share, update transitions
- gesture transitions
- React state animation with View Transitions

## Execution Rule

- Follow `react-view-transition/SKILL.md` exactly as the primary reference.
- Do not duplicate or override its architecture guidance in this file.
- If there is any conflict, `react-view-transition/SKILL.md` wins.

## Maintenance Rule

- Keep deep architecture content only in `react-view-transition/SKILL.md`.
- Keep `AGENT.md` as a thin router/invocation policy only.
